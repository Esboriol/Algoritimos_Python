print("Hola iremos calcular a area do retangulo desejado")

lado = float(input("Digite o valor do lado: "))

base = float(input("Digite o valor da base: "))

area = lado * base

print(f"O valor da area sera de {area:.2f}")